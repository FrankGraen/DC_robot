1. 在当前的末端轨迹运动的执行部分，加入前置初始化阶段，回到以上定义的竖直初始位置后，再开始
2. 编写一个程序，具有GUI，包含以下功能：
  1. 根据当前的DH参数表，允许通过滑块调整各关节角度，并通过预览框实时展示模拟的位姿
  2. 可以一键读取机械臂各个关节的实际角度，并展示在可视化界面和滑块上
  3. 可以指定设备端口，并通过按钮实现指定位姿的串口指令输出。执行模式下，实时对比仿真的预期角度与读取角度是否一致，如果差距大于阈值，认定运行异常，终止运动并反馈